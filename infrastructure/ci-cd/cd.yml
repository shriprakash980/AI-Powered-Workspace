name: DevPilot AI CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  deploy-containers:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Docker Images
        run: |
          echo "Building frontend and production containers..."
          docker compose build --no-cache
